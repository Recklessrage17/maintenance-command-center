import { FormEvent, useEffect, useState } from 'react';
const roles = ['Admin','Manager','Maintenance Tech 3','Maintenance Tech 2','Maintenance Tech 1'];
async function api(path:string, options:RequestInit={}) { const res=await fetch(path,{credentials:'include',headers:{'Content-Type':'application/json'},...options}); const data=await res.json().catch(()=>({})); if(!res.ok) throw new Error(data.error || 'Request failed.'); return data; }
type User={id:number;fullName:string;email:string;role:string;disabled:boolean;lastLoginAt?:string};
export function UsersPage(){ const [users,setUsers]=useState<User[]>([]),[msg,setMsg]=useState(''),[form,setForm]=useState({fullName:'',email:'',role:'Maintenance Tech 1',temporaryPassword:''}); const load=()=>api('/api/users').then(d=>setUsers(d.users)).catch(e=>setMsg(e.message)); useEffect(()=>{ void load(); },[]); async function create(e:FormEvent){e.preventDefault();try{await api('/api/users',{method:'POST',body:JSON.stringify(form)});setForm({fullName:'',email:'',role:'Maintenance Tech 1',temporaryPassword:''});load();}catch(err){setMsg((err as Error).message)}} async function toggle(u:User){await api(`/api/users/${u.id}/${u.disabled?'enable':'disable'}`,{method:'POST'});load()} return <div className="page-stack"><div className="page-heading"><p className="eyebrow">Admin / Users</p><h2>User Management</h2><p>Create local MCC users, assign roles, disable accounts, and review login readiness.</p></div><form className="mcc-card user-form" onSubmit={create}><input placeholder="Full name" value={form.fullName} onChange={e=>setForm({...form,fullName:e.target.value})}/><input placeholder="Email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><select value={form.role} onChange={e=>setForm({...form,role:e.target.value})}>{roles.map(r=><option key={r}>{r}</option>)}</select><input placeholder="Temporary password" type="password" value={form.temporaryPassword} onChange={e=>setForm({...form,temporaryPassword:e.target.value})}/><button className="primary-button">Create User</button></form>{msg&&<p className="form-message error">{msg}</p>}<div className="mcc-card table-card"><table><thead><tr><th>Name</th><th>Email</th><th>Role</th><th>Last login</th><th>Status</th><th></th></tr></thead><tbody>{users.map(u=><tr key={u.id}><td>{u.fullName}</td><td>{u.email}</td><td>{u.role}</td><td>{u.lastLoginAt ?? 'Never'}</td><td>{u.disabled?'Disabled':'Active'}</td><td><button className="secondary-button" onClick={()=>toggle(u)}>{u.disabled?'Enable':'Disable'}</button></td></tr>)}</tbody></table></div></div>}
