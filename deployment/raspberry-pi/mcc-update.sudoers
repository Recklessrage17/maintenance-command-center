# Validate with visudo -cf before installing as /etc/sudoers.d/mcc-update.
# The MCC web account can start only this exact fixed request unit.
mcc ALL=(root) NOPASSWD: /usr/bin/systemctl start mcc-update-request.service
