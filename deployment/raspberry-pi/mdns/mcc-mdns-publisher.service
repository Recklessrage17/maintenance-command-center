[Unit]
Description=Publish the canonical MCC mDNS alias without replacing the Pi reverse identity
Wants=network-online.target
Requires=avahi-daemon.service
After=network-online.target avahi-daemon.service

[Service]
Type=notify
# systemd-notify is a child of the main Bash wrapper. Allow notifications only
# from this service's cgroup; the wrapper explicitly attributes them to its PID.
NotifyAccess=all
ExecStart=/usr/local/sbin/publish-mcc-mdns
Restart=on-failure
RestartPreventExitStatus=78
RestartSec=5s
TimeoutStartSec=30s
User=root
Group=root
NoNewPrivileges=true
PrivateTmp=true
ProtectHome=true
ProtectSystem=strict
ProtectKernelTunables=true
ProtectKernelModules=true
ProtectControlGroups=true
RestrictSUIDSGID=true
CapabilityBoundingSet=

[Install]
WantedBy=multi-user.target
