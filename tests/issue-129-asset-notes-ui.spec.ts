import {expect,type Page,test} from '@playwright/test';

const machine={id:129,assetNumber:'PRESS-129',assetName:'UI Modernization Press',brand:'MCC',model:'MOD-129',serialNumber:'SERIAL-129',machineYear:'2023',location:'North Cell',department:'Molding',status:'active',brandColorHex:'#44D7FF',pmSummary:{total:0,status:'current',label:'PM: Current'},historyPreview:[],createdAt:'2026-09-01T12:00:00Z',updatedAt:'2026-09-15T12:00:00Z'};
const equipment={id:229,assetNumber:'EQ-229',equipmentName:'UI Modernization Dryer',assetName:'UI Modernization Dryer',category:'Dryer',equipmentType:'Desiccant Dryer',manufacturer:'MCC',brand:'MCC',model:'MOD-229',serialNumber:'SERIAL-229',equipmentYear:'2023',year:'2023',location:'South Cell',department:'Molding',status:'active',criticality:'normal',powerType:'Electric',voltage:'480 VAC',phase:'3 phase',amperage:'30 A',airRequirement:'N/A',waterRequirement:'N/A',capacityRating:'200 lb',dimensions:'40 x 40 x 70 in',weight:'500 lb',specificationNotes:'N/A',pmSummary:{total:0,status:'current',label:'PM: Current'},latestHistory:null,createdAt:'2026-09-01T12:00:00Z',updatedAt:'2026-09-15T12:00:00Z'};
const permissions={canEdit:true,canDelete:true,canResolve:false,canReopen:false,canAddUpdate:false,canDeleteAttachments:true};

function note(library:'machine'|'equipment'){
  const asset=library==='machine'?machine:equipment;const noteId=library==='machine'?1291:2291;
  return {id:noteId,assetId:asset.id,title:'Modern attachment reference',noteDate:'2026-09-15',body:'Focused Issue 129 Asset Notes UI regression fixture.',warning:false,createdBy:'Issue 129 Tester',createdAt:'2026-09-15T15:30:00Z',updatedAt:'2026-09-15T16:30:00Z',pdfFilename:`${asset.assetNumber}_Modern_Note.pdf`,pdfUrl:`/api/${library}-library/asset-notes/${noteId}/pdf`,pdfDownloadUrl:`/api/${library}-library/asset-notes/${noteId}/pdf?download=true`,permissions,attachments:[{id:1,noteId,filename:'electrical-inspection.pdf',mimeType:'application/pdf',fileSize:4096,createdAt:'2026-09-15T15:45:00Z',contentUrl:`/api/${library}-library/asset-note-attachments/1/file`,downloadUrl:`/api/${library}-library/asset-note-attachments/1/file?download=true`},{id:2,noteId,filename:'nameplate-photo.webp',mimeType:'image/webp',fileSize:15360,createdAt:'2026-09-15T15:50:00Z',contentUrl:`/api/${library}-library/asset-note-attachments/2/file`,downloadUrl:`/api/${library}-library/asset-note-attachments/2/file?download=true`}],updates:[],lifecycle:[]};
}
function maintenanceIssue(library:'machine'|'equipment',status:'active'|'resolved'='active'){
  const asset=library==='machine'?machine:equipment;const noteId=library==='machine'?1299:2299;const resolved=status==='resolved';
  return {...note(library),id:noteId,title:`${library} drive repair`,body:'Original bearing vibration and drive noise must remain hidden while collapsed.',warning:true,workOrder:`WO-${library.toUpperCase()}-129`,status,createdByUserId:1,resolvedAt:resolved?'2026-09-16T18:00:00Z':null,resolvedBy:resolved?'Issue 129 Tester':'',resolutionSummary:resolved?'Repair verified under production load.':'',resolvedYear:resolved?'2026':'',pdfFilename:`${asset.assetNumber}_Maintenance_Record.pdf`,labor:[{id:1,userId:1,displayName:'Issue 129 Tester',hours:1.25,isPrimary:true,order:0},{id:2,userId:2,displayName:'Second Technician',hours:2.5,isPrimary:false,order:1}],totalLaborHours:3.75,workOrderPages:[{id:90,noteId,filename:'camera-source-name.pdf',mimeType:'application/pdf',fileSize:8192,createdAt:'2026-09-15T15:48:00Z',sourceType:'work_order_photo',sha256:'a'.repeat(64),contentUrl:`/api/${library}-library/asset-note-attachments/90/file`,downloadUrl:`/api/${library}-library/asset-note-attachments/90/file?download=true`}],permissions:{...permissions,canResolve:!resolved,canReopen:resolved,canAddUpdate:!resolved},attachments:[{...note(library).attachments[0],id:91,noteId,filename:'supporting-service-report.pdf'}]};
}

async function commonRoutes(page:Page){
  await page.route('**/api/auth/status',route=>route.fulfill({json:{setupRequired:false,user:{id:1,fullName:'Issue 129 Tester',email:'issue129@example.com',role:'Admin',isOwnerAdmin:true,forcePasswordChange:false,effectivePermissions:['machine.view','machine.notes_manage','machine.import_export','equipment.view','equipment.notes_manage','equipment.import_export']}}}));
  await page.route('**/api/requisitions/summary',route=>route.fulfill({json:{ok:true,requestedCount:0,orderedCount:0,receivedCount:0,canceledCount:0,activeCount:0}}));
  await page.route(/\/api\/machine-library\/assets(?:\?.*)?$/,route=>route.fulfill({json:{ok:true,assets:[machine],brandSettings:[],permissions:{canEdit:true,canDelete:true}}}));
  await page.route(/\/api\/machine-library\/assets\/129\/history$/,route=>route.fulfill({json:{ok:true,asset:machine,records:[]}}));
  await page.route(/\/api\/machine-library\/assets\/129\/(inspection-records|component-images)$/,route=>route.fulfill({json:{ok:true,records:[],images:[]}}));
  await page.route(/\/api\/machine-library\/assets\/129\/preventive-maintenance$/,route=>route.fulfill({json:{ok:true,tasks:[],summary:{total:0,current:0,dueSoon:0,dueNow:0,overdue:0,hold:0,inactive:0,incomplete:0,nextDueDate:null,nextDueMeter:null}}}));
  await page.route(/\/api\/machine-library\/assets\/129\/(document-folders|documents)$/,route=>route.fulfill({json:{ok:true,folders:[],documents:[],summary:{folderCount:0,documentCount:0}}}));
  await page.route(/\/api\/equipment-library\/assets(?:\?.*)?$/,route=>route.fulfill({json:{ok:true,assets:[equipment],categories:['Dryer'],permissions:{canEdit:true,canDelete:true}}}));
  await page.route(/\/api\/equipment-library\/assets\/229\/history$/,route=>route.fulfill({json:{ok:true,asset:equipment,records:[]}}));
  await page.route(/\/api\/equipment-library\/assets\/229\/preventive-maintenance$/,route=>route.fulfill({json:{ok:true,tasks:[],summary:{total:0,dueSoon:0,overdue:0,nextDueDate:null,nextDueMeter:null}}}));
  await page.route(/\/api\/equipment-library\/assets\/229\/(document-folders|documents)$/,route=>route.fulfill({json:{ok:true,folders:[],documents:[],summary:{folderCount:0,documentCount:0}}}));
  await page.route(/\/api\/(machine|equipment)-library\/asset-note-attachments\/2\/file(?:\?.*)?$/,route=>route.fulfill({contentType:'image/png',body:Buffer.from('iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAQAAAC1HAwCAAAAC0lEQVR42mNk+A8AAQUBAScY42YAAAAASUVORK5CYII=','base64')}));
}

async function openNotes(page:Page,library:'machine'|'equipment',withNote=true,records?:ReturnType<typeof note>[]){
  await commonRoutes(page);const asset=library==='machine'?machine:equipment;
  await page.route(new RegExp(`/api/${library}-library/assets/${asset.id}/notes$`),route=>route.fulfill({json:{ok:true,notes:records??(withNote?[note(library)]:[]),currentUser:{id:1,fullName:'Issue 129 Tester',email:'issue129@example.com',role:'Admin',isCurrentUser:true},permissions:{canCreate:true,canExportWorkOrderRecords:true}}}));
  await page.goto(`/${library}-library?asset=${asset.id}`);
  await page.getByRole('button',{name:/Asset Notes & Attachments/}).click();
}

for(const library of ['machine','equipment'] as const){
  test(`${library} Asset Notes uses compact accessible attention status and preserves photo PDF entry`,async({page})=>{
    await openNotes(page,library,false);await page.getByRole('button',{name:'Add Note'}).click();
    const status=page.locator('.asset-note-warning-field');const toggle=page.getByRole('checkbox',{name:/Needs Attention/});
    await expect(status).toHaveAttribute('data-state','off');await expect(status).toContainText('Standard note · alert is off');await expect(status.getByText('Off',{exact:true})).toBeVisible();await expect(toggle).not.toBeChecked();
    const offStyle=await status.evaluate(element=>({border:getComputedStyle(element).borderColor,background:getComputedStyle(element).backgroundImage,color:getComputedStyle(element.querySelector('.asset-note-warning-copy strong')!).color}));
    await toggle.check();await expect(status).toHaveAttribute('data-state','on');await expect(status).toContainText('Dashboard maintenance alert is on');await expect(status.getByText('On',{exact:true})).toBeVisible();await expect(toggle).toBeChecked();
    const onStyle=await status.evaluate(element=>({border:getComputedStyle(element).borderColor,background:getComputedStyle(element).backgroundImage,color:getComputedStyle(element.querySelector('.asset-note-warning-copy strong')!).color}));expect(onStyle).not.toEqual(offStyle);
    await expect(page.getByRole('button',{name:'Take Work Order Photo'})).toBeVisible();await expect(page.getByLabel('Take Asset Notes work-order photo')).toHaveAttribute('capture','environment');
    expect(await page.evaluate(()=>document.documentElement.scrollWidth-document.documentElement.clientWidth)).toBeLessThanOrEqual(1);
  });

  test(`${library} Asset Notes file cards expose lifecycle-appropriate actions and metadata`,async({page})=>{
    await openNotes(page,library);const asset=library==='machine'?machine:equipment;
    const generated=page.getByLabel(`Generated note PDF ${asset.assetNumber}_Modern_Note.pdf`);await expect(generated).toContainText(`${asset.assetNumber}_Modern_Note.pdf`);await expect(generated).toContainText('PDF · Generated note');await expect(generated).toContainText('Updated');await expect(generated.getByRole('button',{name:'Preview'})).toBeVisible();await expect(generated.getByRole('button',{name:'Download'})).toBeVisible();
    const attachment=page.getByLabel('Attachment electrical-inspection.pdf');await expect(attachment).toContainText('PDF · 4.0 KB');await expect(attachment).toContainText('Added');for(const action of ['Preview','Download','Remove'])await expect(attachment.getByRole('button',{name:action,exact:true})).toBeVisible();
    const image=page.getByLabel('Attachment nameplate-photo.webp');await expect(image).toContainText('WEBP · 15 KB');await expect(image.locator('img.asset-attachment-thumbnail')).toHaveAttribute('loading','lazy');
    await generated.getByRole('button',{name:'Preview'}).click();await expect(page.getByRole('dialog',{name:`${asset.assetNumber}_Modern_Note.pdf viewer`})).toBeVisible();await page.getByRole('dialog',{name:`${asset.assetNumber}_Modern_Note.pdf viewer`}).getByRole('button',{name:'Close'}).first().click();
    expect(await page.evaluate(()=>document.documentElement.scrollWidth-document.documentElement.clientWidth)).toBeLessThanOrEqual(1);
  });
}

test('staged Asset Notes cards preview, replace, and remove without changing saved attachment APIs',async({page})=>{
  await openNotes(page,'machine',false);await page.getByRole('button',{name:'Add Note'}).click();const picker=page.locator('.asset-note-attachment-picker input[type=file]');
  await picker.setInputFiles({name:'first-draft.pdf',mimeType:'application/pdf',buffer:Buffer.from('%PDF-1.4 issue 129')});const card=page.getByLabel('Pending attachment first-draft.pdf');await expect(card.getByRole('button',{name:'Preview'})).toBeVisible();await expect(card.getByRole('button',{name:'Replace'})).toBeVisible();await expect(card.getByRole('button',{name:'Remove'})).toBeVisible();
  await card.getByLabel('Replace first-draft.pdf').setInputFiles({name:'final-draft.pdf',mimeType:'application/pdf',buffer:Buffer.from('%PDF-1.4 replacement')});await expect(page.getByLabel('Pending attachment final-draft.pdf')).toBeVisible();await page.getByLabel('Pending attachment final-draft.pdf').getByRole('button',{name:'Remove'}).click();await expect(page.getByLabel('Pending attachment final-draft.pdf')).toHaveCount(0);
});

test('saved attachment cards retain permission-gated destructive actions',async({page})=>{
  await commonRoutes(page);const readOnlyNote={...note('machine'),permissions:{...permissions,canEdit:false,canDelete:false,canDeleteAttachments:false}};await page.route(/\/api\/machine-library\/assets\/129\/notes$/,route=>route.fulfill({json:{ok:true,notes:[readOnlyNote],permissions:{canCreate:false,canExportWorkOrderRecords:false}}}));await page.goto('/machine-library?asset=129');await page.getByRole('button',{name:/Asset Notes & Attachments/}).click();const attachment=page.getByLabel('Attachment electrical-inspection.pdf');await expect(attachment.getByRole('button',{name:'Preview'})).toBeVisible();await expect(attachment.getByRole('button',{name:'Download'})).toBeVisible();await expect(attachment.getByRole('button',{name:'Remove'})).toHaveCount(0);await expect(page.getByRole('button',{name:'Add Note'})).toHaveCount(0);
});

test('Asset Notes file cards stay within a tablet viewport',async({page})=>{
  await page.setViewportSize({width:820,height:1180});await openNotes(page,'equipment');const cards=page.locator('.asset-attachment-chip');await expect(cards).toHaveCount(3);const geometry=await cards.evaluateAll(elements=>elements.map(element=>{const box=element.getBoundingClientRect();return{left:box.left,right:box.right,width:box.width};}));for(const box of geometry){expect(box.left).toBeGreaterThanOrEqual(0);expect(box.right).toBeLessThanOrEqual(820);expect(box.width).toBeGreaterThan(0);}expect(await page.evaluate(()=>document.documentElement.scrollWidth-document.documentElement.clientWidth)).toBeLessThanOrEqual(1);
});

for(const library of ['machine','equipment'] as const){
  test(`${library} maintenance record is compact when collapsed and structured when expanded`,async({page},testInfo)=>{
    await openNotes(page,library,true,[maintenanceIssue(library)]);const card=page.locator('.asset-note-active-issue-card');const summary=card.locator('.asset-note-issue-toggle');
    await expect(summary).toHaveAttribute('aria-expanded','false');await expect(summary).toContainText(`WO-${library.toUpperCase()}-129`);await expect(summary).toContainText(`${library} drive repair`);await expect(summary).toContainText('Active Issue');await expect(summary).toContainText('Warning / Needs Attention');await expect(summary).not.toContainText('Original bearing vibration');await expect(card.locator('.asset-note-body-preview')).not.toBeVisible();
    const collapsed=await summary.evaluate(element=>{const box=element.getBoundingClientRect();return{height:box.height,right:box.right,viewport:innerWidth,overflow:document.documentElement.scrollWidth-document.documentElement.clientWidth};});expect(collapsed.height).toBeLessThanOrEqual(testInfo.project.name==='mobile-chromium'?82:70);expect(collapsed.right).toBeLessThanOrEqual(collapsed.viewport);expect(collapsed.overflow).toBeLessThanOrEqual(1);
    const warningBadge=summary.locator('.asset-note-warning-badge');const warningMetrics=await warningBadge.evaluate(element=>{const box=element.getBoundingClientRect();const icon=getComputedStyle(element,'::before');return{height:box.height,align:getComputedStyle(element).alignItems,lineHeight:getComputedStyle(element).lineHeight,iconWidth:Number.parseFloat(icon.width),iconHeight:Number.parseFloat(icon.height),iconFontSize:Number.parseFloat(icon.fontSize)};});expect(warningMetrics.height).toBeLessThanOrEqual(19);expect(warningMetrics.align).toBe('center');expect(warningMetrics.iconWidth).toBeLessThanOrEqual(10);expect(warningMetrics.iconHeight).toBeLessThanOrEqual(10);expect(warningMetrics.iconFontSize).toBeLessThan(8);
    await summary.click();await expect(summary).toHaveAttribute('aria-expanded','true');for(const section of ['Status / Lifecycle','Work Order Information','Issue / Technician Comments','Labor / Technicians','PDF Reports / Supporting Attachments'])await expect(card.getByText(section,{exact:true})).toBeVisible();await expect(card.locator('.asset-note-body-preview')).toHaveText('Original bearing vibration and drive noise must remain hidden while collapsed.');await expect(card.getByText('Issue 129 Tester',{exact:true})).toBeVisible();await expect(card.getByText('Second Technician',{exact:true})).toBeVisible();await expect(card.getByText('3.75 total hours')).toBeVisible();const photos=card.getByLabel('1 photographed work-order PDF included in maintenance record');await expect(photos).toContainText('appended after the record summary');await expect(card.getByText('camera-source-name.pdf')).toHaveCount(0);await expect(card.getByLabel('Attachment supporting-service-report.pdf')).toBeVisible();expect(await page.evaluate(()=>document.documentElement.scrollWidth-document.documentElement.clientWidth)).toBeLessThanOrEqual(1);
  });
}

test('collapsed maintenance summary uses one deliberate row when wide and two rows on mobile',async({page})=>{
  await openNotes(page,'machine',true,[maintenanceIssue('machine')]);const summary=page.locator('.asset-note-issue-toggle');
  for(const viewport of [{name:'desktop',width:1440,height:900},{name:'tablet',width:820,height:1180},{name:'mobile landscape',width:844,height:390},{name:'mobile portrait',width:390,height:844}]){
    await page.setViewportSize({width:viewport.width,height:viewport.height});await expect(summary).toBeVisible();
    const layout=await summary.evaluate(element=>{const values=(box:DOMRect)=>({left:box.left,right:box.right,top:box.top,bottom:box.bottom,width:box.width,height:box.height,centerY:box.top+box.height/2});const rect=(selector:string)=>values(element.querySelector(selector)!.getBoundingClientRect());return{summary:values(element.getBoundingClientRect()),badges:rect('.asset-note-issue-badges'),identity:rect('.asset-note-summary-identity'),chevron:rect('.asset-note-issue-chevron'),title:rect('.asset-note-issue-title'),overflow:document.documentElement.scrollWidth-document.documentElement.clientWidth};});
    expect(layout.summary.right).toBeLessThanOrEqual(viewport.width);expect(layout.overflow,`${viewport.name} horizontal overflow`).toBeLessThanOrEqual(1);expect(layout.title.width).toBeGreaterThan(20);
    if(viewport.width<=560){expect(layout.identity.top,`${viewport.name} identity row`).toBeGreaterThanOrEqual(layout.badges.bottom+3);expect(Math.abs(layout.badges.centerY-layout.chevron.centerY),`${viewport.name} status/chevron alignment`).toBeLessThanOrEqual(4);expect(layout.identity.right).toBeLessThanOrEqual(layout.summary.right-6);}
    else{expect(Math.abs(layout.badges.centerY-layout.identity.centerY),`${viewport.name} horizontal alignment`).toBeLessThanOrEqual(4);expect(layout.identity.left,`${viewport.name} status/identity spacing`).toBeGreaterThanOrEqual(layout.badges.right+6);expect(layout.chevron.left,`${viewport.name} identity/chevron spacing`).toBeGreaterThanOrEqual(layout.identity.right+6);}
  }
});

test('edit mode is visually distinct, lifecycle locked, and calculates multi-technician labor',async({page})=>{
  await commonRoutes(page);await page.route(/\/api\/machine-library\/asset-note-technicians(?:\?.*)?$/,route=>route.fulfill({json:{ok:true,technicians:[{id:1,fullName:'Issue 129 Tester',email:'issue129@example.com',role:'Admin',isCurrentUser:true},{id:2,fullName:'Second Technician',email:'second@example.com',role:'Maintenance Tech 3'}]}}));await page.route(/\/api\/machine-library\/assets\/129\/notes$/,route=>route.fulfill({json:{ok:true,notes:[maintenanceIssue('machine')],currentUser:{id:1,fullName:'Issue 129 Tester',email:'issue129@example.com',role:'Admin'},permissions:{canCreate:true,canExportWorkOrderRecords:true}}}));await page.goto('/machine-library?asset=129');await page.getByRole('button',{name:/Asset Notes & Attachments/}).click();const card=page.locator('.asset-note-active-issue-card');await card.locator('.asset-note-issue-toggle').click();await card.getByRole('button',{name:/More actions for machine drive repair/}).click();await page.getByRole('menuitem',{name:'Edit Note'}).click();const editor=page.locator('.asset-note-form.is-editing');await expect(editor).toContainText('EDITING');const locked=editor.getByRole('status',{name:'Needs Attention active and lifecycle locked'});await expect(locked).toContainText('Active · Locked');await expect(editor.getByRole('checkbox',{name:/Needs Attention/})).toHaveCount(0);const lockedMetrics=await locked.evaluate(element=>{const box=element.getBoundingClientRect();const icon=element.querySelector('svg')!.getBoundingClientRect();const label=element.querySelector('strong')!.getBoundingClientRect();return{height:box.height,iconWidth:icon.width,iconHeight:icon.height,centerDelta:Math.abs((icon.top+icon.height/2)-(label.top+label.height/2)),fontSize:Number.parseFloat(getComputedStyle(element).fontSize),padding:getComputedStyle(element).padding};});expect(lockedMetrics.height).toBeLessThanOrEqual(25);expect(lockedMetrics.iconWidth).toBeLessThanOrEqual(10);expect(lockedMetrics.iconHeight).toBeLessThanOrEqual(10);expect(lockedMetrics.centerDelta).toBeLessThanOrEqual(1.5);expect(lockedMetrics.fontSize).toBeLessThan(10);const visual=await editor.evaluate(element=>({border:getComputedStyle(element).borderColor,background:getComputedStyle(element).backgroundImage,inset:getComputedStyle(element).boxShadow}));expect(visual.border).not.toBe('rgba(0, 0, 0, 0)');expect(visual.background).not.toBe('none');expect(visual.inset).toContain('inset');await expect(editor.getByLabel('Hours for Issue 129 Tester')).toHaveValue('1.25');await expect(editor.getByLabel('Hours for Second Technician')).toHaveValue('2.5');await editor.getByLabel('Hours for Second Technician').fill('3.75');await expect(editor.locator('.asset-note-labor-total output')).toHaveText('5.00');await editor.getByRole('button',{name:'Remove Second Technician'}).click();await expect(editor.locator('.asset-note-labor-total output')).toHaveText('1.25');await editor.getByRole('button',{name:'+ Add Technician'}).click();await editor.getByLabel('Additional Technician').selectOption('2');await editor.getByRole('button',{name:'Add',exact:true}).click();await expect(editor.getByLabel('Hours for Second Technician')).toHaveValue('0');await editor.getByRole('button',{name:'Make Primary'}).click();await expect(editor.getByText('Primary Technician',{exact:true})).toHaveCount(1);
});

test('Work Order History exposes Active, Resolved, and All filters with printable records',async({page})=>{
  const resolved={...maintenanceIssue('machine','resolved'),id:1300,title:'resolved drive repair'};await openNotes(page,'machine',true,[maintenanceIssue('machine'),resolved]);const historyButton=page.getByRole('button',{name:/Work Order History/});await expect(historyButton).toBeVisible();await historyButton.click();const history=page.getByRole('dialog',{name:'Resolved History for PRESS-129'});await expect(history.getByText('Work Order History',{exact:true})).toBeVisible();const filter=history.getByLabel('Work Order History status');await expect(filter).toHaveValue('resolved');const year=history.getByRole('button',{name:/2026.*1 resolved issue/});await year.click();await expect(history.getByRole('button',{name:/resolved drive repair/})).toBeVisible();await filter.selectOption('active');const active=history.getByRole('button',{name:/Active.*1 active issue/});await expect(active).toHaveAttribute('aria-expanded','true');await expect(history.getByRole('button',{name:/machine drive repair/})).toBeVisible();await filter.selectOption('all');await expect(history.getByRole('button',{name:/2026.*1 resolved issue/})).toBeVisible();await expect(history.getByRole('button',{name:/Active.*1 active issue/})).toBeVisible();expect(await history.evaluate(()=>document.documentElement.scrollWidth-document.documentElement.clientWidth)).toBeLessThanOrEqual(1);
});

test('maintenance records remain overflow-free in mobile landscape',async({page})=>{
  await page.setViewportSize({width:844,height:390});await openNotes(page,'equipment',true,[maintenanceIssue('equipment')]);const summary=page.locator('.asset-note-issue-toggle');await expect(summary).toBeVisible();await summary.click();const sections=page.locator('.asset-note-record-section');await expect(sections).toHaveCount(5);const geometry=await sections.evaluateAll(elements=>elements.map(element=>{const box=element.getBoundingClientRect();return{left:box.left,right:box.right};}));for(const box of geometry){expect(box.left).toBeGreaterThanOrEqual(0);expect(box.right).toBeLessThanOrEqual(844);}expect(await page.evaluate(()=>document.documentElement.scrollWidth-document.documentElement.clientWidth)).toBeLessThanOrEqual(1);
});
