import assert from 'node:assert/strict';
import {spawn} from 'node:child_process';
import fs from 'node:fs';
import net from 'node:net';
import path from 'node:path';
import {DatabaseSync} from 'node:sqlite';
import {fileURLToPath} from 'node:url';

const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');
const fixture=path.join(root,'tmp',`warning-issue-lifecycle-${Date.now()}-${process.pid}`);
const dataDir=path.join(fixture,'data');
const uploadsDir=path.join(fixture,'uploads');
const backupsDir=path.join(fixture,'backups');
const password='Warning-Issue-Test!9';
let server;

async function freePort(){return new Promise((resolve,reject)=>{const probe=net.createServer();probe.once('error',reject);probe.listen(0,'127.0.0.1',()=>{const address=probe.address();probe.close(error=>error?reject(error):resolve(address.port));});});}
async function start(port){const child=spawn(process.execPath,['backend/dist/server/index.js'],{cwd:root,env:{...process.env,PORT:String(port),NODE_ENV:'test',SESSION_SECRET:'warning-issue-lifecycle-test',MCC_DATA_DIR:dataDir,MCC_UPLOADS_DIR:uploadsDir,MCC_BACKUPS_DIR:backupsDir},stdio:['ignore','pipe','pipe']});let output='';child.stdout.on('data',chunk=>output+=chunk);child.stderr.on('data',chunk=>output+=chunk);const base=`http://127.0.0.1:${port}`;for(let attempt=0;attempt<300;attempt+=1){if(child.exitCode!==null)throw new Error(`Backend exited.\n${output}`);try{if((await fetch(`${base}/api/health`)).ok)return{child,base};}catch{}await new Promise(resolve=>setTimeout(resolve,100));}throw new Error(`Backend did not start.\n${output}`);}
async function request(base,pathname,{method='GET',cookie='',body,headers={}}={}){const response=await fetch(`${base}${pathname}`,{method,headers:{...(cookie?{Cookie:cookie}:{}),...(body===undefined||body instanceof FormData?{}:{'Content-Type':'application/json'}),...headers},body:body===undefined?undefined:body instanceof FormData?body:JSON.stringify(body)});const data=await response.json().catch(()=>({}));return{response,data,cookie:response.headers.get('set-cookie')?.split(';')[0]||''};}
async function login(base,email){const result=await request(base,'/api/auth/login',{method:'POST',body:{email,password}});assert.equal(result.response.status,200,JSON.stringify(result.data));return result.cookie;}
function noteForm({title,body,warning=true,workOrder='',attachment=false}){const form=new FormData();form.append('title',title);form.append('noteDate','2026-08-20');form.append('body',body);form.append('warning',String(warning));if(workOrder)form.append('workOrder',workOrder);if(attachment)form.append('attachments',new Blob([Buffer.from('%PDF-1.4\nWarning issue fixture\n%%EOF\n')],{type:'application/pdf'}),'initial-inspection.pdf');return form;}
function updateForm(body,attachment=false){const form=new FormData();form.append('body',body);if(attachment)form.append('attachments',new Blob([Buffer.from('%PDF-1.4\nMaintenance update fixture\n%%EOF\n')],{type:'application/pdf'}),'repair-evidence.pdf');return form;}
async function dashboardIssues(base,cookie,library,assetId){const result=await request(base,'/api/dashboard/preventive-maintenance-due',{cookie});assert.equal(result.response.status,200,JSON.stringify(result.data));return result.data.warningNotes.filter(note=>note.assetLibrary===library&&note.assetId===assetId);}
async function editIssue(base,cookie,library,note,overrides={}){return request(base,`/api/${library}-library/asset-notes/${note.id}`,{method:'PUT',cookie,body:noteForm({title:overrides.title??note.title,body:overrides.body??note.body,warning:true,workOrder:overrides.workOrder??note.workOrder})});}

async function exerciseLibrary({base,library,assetId,creatorCookie,otherCookie,updaterCookie,managerCookie,adminCookie,deleteCookie}){
  const label=library==='machine'?'Machine':'Equipment';const workOrder=`WO-98-${library.toUpperCase()}`;const originalBody=`${label} original problem statement must remain unchanged.`;
  let result=await request(base,`/api/${library}-library/assets/${assetId}/notes`,{method:'POST',cookie:creatorCookie,body:noteForm({title:`${label} warning issue`,body:originalBody,workOrder,attachment:true})});assert.equal(result.response.status,201,JSON.stringify(result.data));let note=result.data.note;assert.equal(note.warning,true);assert.equal(note.status,'active');assert.equal(note.workOrder,workOrder);assert.equal(note.permissions.canEdit,true);assert.equal(note.permissions.canDelete,false,'Standard creator must not delete warning issues.');assert.equal(note.attachments.length,1);

  result=await request(base,`/api/${library}-library/assets/${assetId}/notes`,{method:'POST',cookie:creatorCookie,body:noteForm({title:`${label} ordinary note`,body:'Ordinary note behavior remains ordinary.',warning:false})});assert.equal(result.response.status,201);const ordinary=result.data.note;assert.equal(ordinary.status,'ordinary');assert.equal(ordinary.workOrder,'');
  assert.equal((await dashboardIssues(base,creatorCookie,library,assetId)).length,1,'Only the active warning issue should reach Dashboard attention.');

  result=await editIssue(base,otherCookie,library,note,{title:'Unauthorized rewrite'});assert.equal(result.response.status,403,'A standard user must not edit another user\'s warning issue.');
  result=await request(base,`/api/${library}-library/asset-note-attachments/${note.attachments[0].id}`,{method:'DELETE',cookie:otherCookie});assert.equal(result.response.status,403,'A standard user must not remove another user\'s warning issue attachments.');
  result=await request(base,`/api/${library}-library/asset-notes/${note.id}`,{method:'DELETE',cookie:otherCookie,body:{deleteReason:'Unauthorized delete attempt'}});assert.equal(result.response.status,403,'A standard user must not delete another user\'s warning issue.');
  result=await request(base,`/api/${library}-library/asset-notes/${note.id}/resolve`,{method:'POST',cookie:otherCookie,body:{resolutionSummary:'Unauthorized resolution'}});assert.equal(result.response.status,403,'A standard non-creator must not resolve another user\'s warning issue.');assert.equal((await dashboardIssues(base,creatorCookie,library,assetId)).length,1,'Unauthorized lifecycle attempts must not manipulate Dashboard state.');

  result=await editIssue(base,creatorCookie,library,note,{title:`${label} creator-edited warning`});assert.equal(result.response.status,200,JSON.stringify(result.data));note=result.data.note;assert.equal(note.title,`${label} creator-edited warning`);assert.equal(note.body,originalBody);
  result=await request(base,`/api/${library}-library/asset-notes/${note.id}/updates`,{method:'POST',cookie:creatorCookie,body:updateForm('Inspected the affected assembly and documented baseline readings.')});assert.equal(result.response.status,201,JSON.stringify(result.data));
  result=await request(base,`/api/${library}-library/asset-notes/${note.id}/updates`,{method:'POST',cookie:updaterCookie,body:updateForm('Replaced the worn component and completed operational testing.',true)});assert.equal(result.response.status,201,JSON.stringify(result.data));note=result.data.note;assert.equal(note.body,originalBody,'Progress updates must not overwrite the original issue body.');assert.deepEqual(note.updates.map(update=>update.body),['Inspected the affected assembly and documented baseline readings.','Replaced the worn component and completed operational testing.']);assert.equal(note.updates[1].createdBy,'Lifecycle Updater');assert.equal(note.updates[1].attachments.length,1);
  const updateAttachment=await fetch(`${base}${note.updates[1].attachments[0].contentUrl}`,{headers:{Cookie:updaterCookie}});assert.equal(updateAttachment.status,200);assert.match(updateAttachment.headers.get('content-type')||'',/application\/pdf/);

  result=await request(base,`/api/${library}-library/warning-issues?q=Lifecycle%20Updater&status=active`,{cookie:creatorCookie});assert.equal(result.response.status,200);assert.equal(result.data.issues.length,1,'Active warning search must include update technician names.');
  result=await request(base,`/api/${library}-library/asset-notes/${note.id}/resolve`,{method:'POST',cookie:creatorCookie,body:{}});assert.equal(result.response.status,400,'Resolution Summary is mandatory.');
  result=await request(base,`/api/${library}-library/asset-notes/${note.id}/resolve`,{method:'POST',cookie:creatorCookie,body:{resolutionSummary:'Repair verified under normal production load.'}});assert.equal(result.response.status,200,JSON.stringify(result.data));note=result.data.note;assert.equal(note.status,'resolved');assert.equal(note.workOrder,workOrder);assert.equal(note.body,originalBody);assert.equal(note.resolutionSummary,'Repair verified under normal production load.');assert.equal((await dashboardIssues(base,creatorCookie,library,assetId)).length,0,'Resolved warning issues must leave Dashboard attention immediately.');
  result=await request(base,`/api/${library}-library/asset-note-attachments/${note.attachments[0].id}`,{method:'DELETE',cookie:creatorCookie});assert.equal(result.response.status,409,'Resolved issue attachments must remain preserved until an authorized reopen.');
  const preservedAttachment=await fetch(`${base}${note.attachments[0].downloadUrl}`,{headers:{Cookie:creatorCookie}});assert.equal(preservedAttachment.status,200,'Resolved issue attachment file must remain reviewable.');
  result=await request(base,`/api/${library}-library/asset-notes/${note.id}/updates`,{method:'POST',cookie:updaterCookie,body:updateForm('This update must be rejected while resolved.')});assert.equal(result.response.status,409);
  result=await request(base,`/api/${library}-library/warning-issues?q=${encodeURIComponent(workOrder)}&status=resolved&year=2026`,{cookie:creatorCookie});assert.equal(result.response.status,200);assert.equal(result.data.issues.length,1,'Work Order must remain searchable after resolution.');assert.deepEqual(result.data.years,['2026']);assert.equal(result.data.issues[0].updates.length,2);
  result=await request(base,`/api/${library}-library/asset-notes/${note.id}/reopen`,{method:'POST',cookie:otherCookie,body:{reopenReason:'Unauthorized reopen'}});assert.equal(result.response.status,403,'A standard non-creator must not reopen another user\'s issue.');assert.equal((await dashboardIssues(base,creatorCookie,library,assetId)).length,0,'Unauthorized reopen must not restore Dashboard attention.');
  result=await request(base,`/api/${library}-library/asset-notes/${note.id}/reopen`,{method:'POST',cookie:creatorCookie,body:{reopenReason:'Verification reading moved outside tolerance.'}});assert.equal(result.response.status,200);note=result.data.note;assert.equal(note.status,'active');assert.equal((await dashboardIssues(base,creatorCookie,library,assetId)).length,1,'Authorized reopening must restore Dashboard attention.');

  result=await editIssue(base,managerCookie,library,note,{title:`${label} manager-edited warning`});assert.equal(result.response.status,200,'Manager must edit any warning issue.');note=result.data.note;
  result=await editIssue(base,adminCookie,library,note,{title:`${label} admin-edited warning`});assert.equal(result.response.status,200,'Admin must edit any warning issue.');note=result.data.note;
  result=await request(base,`/api/${library}-library/asset-notes/${note.id}/resolve`,{method:'POST',cookie:managerCookie,body:{resolutionSummary:'Manager confirmed the follow-up verification.'}});assert.equal(result.response.status,200);note=result.data.note;
  result=await request(base,`/api/${library}-library/asset-notes/${note.id}`,{method:'DELETE',cookie:deleteCookie,body:{}});assert.equal(result.response.status,400,'Delete Reason is mandatory for warning issues.');
  result=await request(base,`/api/${library}-library/asset-notes/${note.id}`,{method:'DELETE',cookie:deleteCookie,body:{deleteReason:`${label} duplicate record retired after audit review.`}});assert.equal(result.response.status,200,JSON.stringify(result.data));assert.equal(result.data.historyPreserved,true);assert.equal((await dashboardIssues(base,creatorCookie,library,assetId)).length,0);

  result=await request(base,`/api/${library}-library/asset-notes/${ordinary.id}`,{method:'DELETE',cookie:creatorCookie});assert.equal(result.response.status,200,'Ordinary note deletion must retain its existing no-reason workflow.');
  return{noteId:note.id,workOrder,originalBody,initialAttachmentReference:note.attachments[0].downloadUrl};
}

async function run(){
  fs.mkdirSync(fixture,{recursive:true});const runtime=await start(await freePort());server=runtime.child;const{base}=runtime;
  let result=await request(base,'/api/auth/setup-first-admin',{method:'POST',body:{fullName:'Lifecycle Admin',email:'admin@example.com',password,confirmPassword:password}});assert.equal(result.response.status,200);const adminCookie=await login(base,'admin@example.com');
  for(const user of [{fullName:'Issue Creator',email:'creator@example.com',role:'Maintenance Tech 3'},{fullName:'Other Standard Tech',email:'other@example.com',role:'Maintenance Tech 3'},{fullName:'Lifecycle Updater',email:'updater@example.com',role:'Maintenance Tech 2'},{fullName:'Lifecycle Manager',email:'manager@example.com',role:'Manager'}]){result=await request(base,'/api/users',{method:'POST',cookie:adminCookie,body:{...user,temporaryPassword:password}});assert.equal(result.response.status,201,JSON.stringify(result.data));}
  const creatorCookie=await login(base,'creator@example.com');const otherCookie=await login(base,'other@example.com');const updaterCookie=await login(base,'updater@example.com');const managerCookie=await login(base,'manager@example.com');
  result=await request(base,'/api/machine-library/assets',{method:'POST',cookie:adminCookie,body:{assetNumber:'ISSUE-98-M',assetName:'Lifecycle Press',brand:'Toyo',model:'M98',serialNumber:'M-98',location:'North Cell',status:'active'}});assert.equal(result.response.status,201,JSON.stringify(result.data));const machineAssetId=result.data.asset.id;
  result=await request(base,'/api/equipment-library/assets',{method:'POST',cookie:adminCookie,body:{assetNumber:'ISSUE-98-E',equipmentName:'Lifecycle Dryer',category:'Dryer',manufacturer:'Matsui',model:'E98',serialNumber:'E-98',location:'South Cell',status:'active'}});assert.equal(result.response.status,201,JSON.stringify(result.data));const equipmentAssetId=result.data.asset.id;

  const machine=await exerciseLibrary({base,library:'machine',assetId:machineAssetId,creatorCookie,otherCookie,updaterCookie,managerCookie,adminCookie,deleteCookie:adminCookie});
  const equipment=await exerciseLibrary({base,library:'equipment',assetId:equipmentAssetId,creatorCookie,otherCookie,updaterCookie,managerCookie,adminCookie,deleteCookie:managerCookie});

  const database=new DatabaseSync(path.join(dataDir,'mcc.sqlite'),{readOnly:true});
  for(const table of ['machine_asset_notes','equipment_asset_notes']){const columns=new Set(database.prepare(`PRAGMA table_info(${table})`).all().map(column=>column.name));for(const column of ['work_order_reference','issue_status','resolved_at','resolved_by_user_id','resolution_summary','reopened_at','reopened_by_user_id','deleted','deleted_at','deleted_by_user_id','delete_reason'])assert.ok(columns.has(column),`${table} migration missing ${column}`);}
  for(const table of ['asset_note_updates','asset_note_update_attachments','asset_note_lifecycle_events'])assert.ok(database.prepare("SELECT name FROM sqlite_master WHERE type='table' AND name=?").get(table),`Missing lifecycle table ${table}`);
  for(const [library,fixtureData] of [['machine',machine],['equipment',equipment]]){const table=`${library}_asset_notes`;const row=database.prepare(`SELECT * FROM ${table} WHERE id=?`).get(fixtureData.noteId);assert.equal(row.deleted,1);assert.match(row.delete_reason,/audit review/i);assert.equal(row.work_order_reference,fixtureData.workOrder);assert.equal(row.body,fixtureData.originalBody);assert.equal(database.prepare('SELECT COUNT(*) AS count FROM asset_note_updates WHERE asset_library=? AND note_id=?').get(library,fixtureData.noteId).count,2);const events=database.prepare('SELECT event_type,reason FROM asset_note_lifecycle_events WHERE asset_library=? AND note_id=? ORDER BY id').all(library,fixtureData.noteId);for(const type of ['issue_created','update_added','issue_resolved','issue_reopened','issue_edited','issue_deleted'])assert.ok(events.some(event=>event.event_type===type),`${library} missing ${type} audit event`);assert.ok(events.some(event=>event.event_type==='issue_deleted'&&/audit review/i.test(event.reason)));}
  const deletionAudits=database.prepare("SELECT target_type,details_json FROM audit_log WHERE action='warning issue delete' ORDER BY id").all();assert.equal(deletionAudits.length,2);assert.deepEqual(deletionAudits.map(row=>row.target_type).sort(),['equipment_asset_note','machine_asset_note']);for(const row of deletionAudits){const details=JSON.parse(row.details_json);assert.equal(details.historyPreserved,true);assert.equal(details.attachmentsPreserved,true);assert.match(details.reason,/audit review/i);}
  database.close();
  console.log('Warning issue lifecycle API tests passed for Machine + Equipment ownership/elevation, append-only updates, attachments, Work Order search, resolve/reopen Dashboard state, delete reasons, soft-delete audit evidence, ordinary-note behavior, and schema migrations.');
}

try{await run();}finally{if(server&&server.exitCode===null){server.kill();await Promise.race([new Promise(resolve=>server.once('exit',resolve)),new Promise(resolve=>setTimeout(resolve,3000))]);}const resolved=path.resolve(fixture);const allowed=path.resolve(root,'tmp');if(resolved.startsWith(`${allowed}${path.sep}`)&&fs.existsSync(resolved))fs.rmSync(resolved,{recursive:true,force:true});}
